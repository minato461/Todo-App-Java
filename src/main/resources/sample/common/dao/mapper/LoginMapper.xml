<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-0/EN/mybatis-3-mapper.dtd">

<mapper namespace="sample.common.dao.mapper.LoginMapper">
	<select id="findUser" resultType="sample.common.dao.entity.Login">
		SELECT
			id,
			username,
			password,
			created_at AS createdAt,
			updated_at AS updatedAt
		FROM
			login
		WHERE
			username = #{username}
	</select>
	
	<insert id="insertUser">
		INSERT INTO login (username, password, created_at, updated_at)
		VALUES (#{username}, #{password}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
	</insert>
	
</mapper>